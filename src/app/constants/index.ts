import { environment } from '../../environments/environment';

export const authorsResourceEndpoint = `${environment.api_host}/authors`;
export const categoriesResourceEndpoint = `${environment.api_host}/categories`;
